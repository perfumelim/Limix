import Header from '~/components/Header'
import HeaderBackButton from '~/components/HeaderBackButton'

const Resister = () => {
  return (
    <div>
      <Header title="회원가입" headerBack={<HeaderBackButton />} />
    </div>
  )
}

export default Resister
